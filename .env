# ============================================
# WAF SafeLine Configuration
# ============================================

# SafeLine Directory - where SafeLine stores its data
SAFELINE_DIR=/home/baivab/repos/waf-safeline/safeline-data

# SafeLine Image Configuration
IMAGE_TAG=latest
IMAGE_PREFIX=chaitin
ARCH_SUFFIX=
RELEASE=
REGION=-g

# SafeLine Management Port (Web Console)
MGT_PORT=9443

# SafeLine PostgreSQL Password
# IMPORTANT: Change this to a strong password!
POSTGRES_PASSWORD=SafeLineStrongPassword123!

# SafeLine Network Configuration
SUBNET_PREFIX=172.22.222

# Management Proxy Layers (0 = no proxy)
MGT_PROXY=0

# ============================================
# n8n Configuration
# ============================================

# n8n Data Directories
N8N_DIR=/home/baivab/repos/waf-safeline/n8n-data
N8N_FILES_DIR=/home/baivab/repos/waf-safeline/n8n-files
N8N_POSTGRES_DIR=/home/baivab/repos/waf-safeline/n8n-postgres-data

# n8n Host Configuration
N8N_HOST=localhost
N8N_PROTOCOL=http
WEBHOOK_URL=http://localhost:5678/

# n8n Database Configuration
N8N_DB_NAME=n8n
N8N_DB_USER=n8n
# IMPORTANT: Change this to a strong password!
N8N_DB_PASSWORD=N8nStrongPassword123!

# n8n Authentication
# IMPORTANT: Change these credentials!
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin123

# Timezone (adjust to your location)
GENERIC_TIMEZONE=America/New_York

# ============================================
# Notes:
# ============================================
# 1. Make sure to change all passwords before deploying
# 2. SafeLine will be accessible at: https://localhost:9443
# 3. n8n will be accessible at: http://localhost:5678
# 4. Both services use their own PostgreSQL instances
# 5. Data is persisted in local directories specified above
